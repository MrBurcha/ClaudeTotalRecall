# Normalize line endings across platforms: text files are stored as LF in the repo
# and checked out as LF on every OS (including Windows), so byte-exact tests, git
# diffs and tooling behave identically regardless of the contributor's core.autocrlf.
# `text=auto` lets git auto-detect binary files and leave them untouched.
* text=auto eol=lf
